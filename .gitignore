# Secrets - never commit
.mcp.json
**/.mcp.json
.env
**/.env

# Reference scraping: raw cached pages/assets (durable per-run cache, not source).
# Distilled blueprints under .reference/<section>/*.md ARE committed.
**/.reference/cache/
